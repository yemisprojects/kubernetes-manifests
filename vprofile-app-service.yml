apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
spec:
  ports:
  - port: 80
    targetPort: vproapp-port
    protocol: TCP
  selector:
    app: vproapp
  type: ClusterIP

# spec:
#   type: NodePort
#   selector:
#     app: app3-nginx
#   ports:
#     - port: 80
#       targetPort: 80